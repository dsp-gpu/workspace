# Changelog — 2026-05

## 2026-05-12 — Offline-pack для рабочего Debian: ROCm 7.2 devkit + onnxruntime 📦

- **ROCm 7.2 devkit `.deb`** скачаны через WSL2 Ubuntu 24.04 (`apt-get install --download-only` из `repo.radeon.com noble`): **76 файлов / 3.7 GB** в `D:\offline-debian-pack\7_dop_files\lib_deb\`. Покрытие: hipcc + hip-runtime-amd + rocm-llvm/cmake + hipfft-dev + rocblas-dev + rocsolver-dev + rocprim-dev + rocrand-dev + транзитивные deps. Системные libc/libstdc++ удалены (используются debian-родные).
- **onnxruntime 1.20.* для cp312 / manylinux_2_28** скачан через `pip download` (для Continue embedding server, Вариант B). `fastembed` пропустили — конфликт зависимостей, пойдём напрямую через `onnxruntime + tokenizers` в своём FastAPI.
- Артефакты: `MemoryBank/tasks/TASK_install_rocm_hip_sdk_debian_2026-05-12.md` (обновлён с инструкцией установки), `MemoryBank/tasks/TASK_continue_embedding_setup.md` (Вариант B зафиксирован).
- **Следующий шаг**: на рабочем Debian — `sudo apt install /path/lib_deb/*.deb` → проверка `hipcc`, `hip-config.cmake`, тестовая сборка `core`.

## 2026-05-09 — RAG-инфра финал: CTX2 + ENRICH_TG + CLAUDE_C4 → dataset_v3 = 2221 пар ✅

- **CTX2 doxygen_test_parser** (БЛОКЕР снят): 219 hpp/8 репо → +645 INSERT +505 UPDATE в `rag_dsp.test_params`. Total 674→**1319** rows; ready_for_autotest 111→**983** (+783%).
- **ENRICH_TG** ✅ DoD вечер: 480/480 test_gen placeholder'ов обогащены реальным C++ через qwen3:8b (0 fail). Финальный `dataset_v3.jsonl` = **2221 пар** (DoD ≥2000 с запасом 11%).
- **CLAUDE_C4** ✅ DoD утра (параллельная сестра): 8/8 `<repo>/CLAUDE.md` C4-блоки + 8/8 `_RAG.md` `tags:` (66 inferred) + 8 pairs `claude_md_section` шаблона.
- **EV.E2 RAGAs + abstain** ✅: faithfulness(grounded)=0.8, hallucination=0.0; 4 метрики через ollama judge.
- **CTX4 mcp_atomic_tools** ✅: 4 MCP tools (`dsp_test_params`/`dsp_use_case`/`dsp_pipeline`/`dsp_doc_block`).
- **DS dataset_v3** ✅: 1093→2020 baseline через 5 шаблонов, потом +193 после CTX2 LEVEL 1, потом +8 после CLAUDE_C4 → **2221 final**.
- **Урок (memory):** долгие скрипты ⇒ heartbeat ПЕРЕД операцией + `flush=True` + `python -u` + парный `watch_*.py` (ENRICH_TG висел 30+с на template-методах, Alex не понимал считает или умерло).
- Артефакты: `sessions/2026-05-09.md`, `specs/rag_ctx2_implementation_review_2026-05-09.md`, `prompts/handoff_session_2026-05-09_morning.md` (rewritten).

## 2026-05-08 — Diagnostic утра: r-up vs dataset clean (парадоксальный результат) 🔬

- **3 эксперимента** на 2080 Ti: Phase A r=4 dirty (44 мин) → Diagnostic r=8 dirty (62 мин) → CLEAN r=8 247 (26 мин).
- **Парадокс**: CLEAN дал last-10 avg loss 0.815 (лучший), но **inference хуже всех** — «Rochester GPU» галлюцинация, потеря namespace `drv_gpu_lib::`, возврат зацикливания.
- **Гипотеза «датасет = bottleneck» (max-5/class) — ОПРОВЕРГНУТА**. Корень: catastrophic forgetting + потеря critical mass повторений факт-имён (1093 → 247 = -77% повторений `hipfft`/`drv_gpu_lib::`).
- **Методический вывод**: train_loss (даже last-N) ≠ inference quality на узкоспециальных датасетах. Только **inference compare** даёт честный judge.
- **Plan для Phase B (12.05) обновлён**: dirty 1093 + r=16 + bf16 baseline, расширение датасета через RAG до 2000+ как параллельный трек. **НЕ использовать clean-247**.
- Артефакты: `MemoryBank/sessions/2026-05-08.md`, `MemoryBank/specs/LLM_and_RAG/finetune_diagnostic_2026-05-08.md`, обновлён `MemoryBank/tasks/TASK_FINETUNE_phase_B_2026-05-12.md`.
- Скрипты: `run_full_qwen3_r8_clean.ps1`, `run_compare_3way.ps1`. Continue MCP проверен (✅ 200 OK).

## 2026-05-07 — QLoRA fine-tune Phase A (план A handoff'а — путь проторён) ✅

- **train_simple.py** (без trl, чистый transformers.Trainer) — фикс silent hang предыдущей ночи. План A зашёл: 411 шагов / 3 эпохи / 44 мин / loss 2.45→1.18.
- **End-to-end pipeline проторён**: `train → merge_lora → convert_hf_to_gguf → quantize Q4_K_M → ollama create`. Модель `qwen3-8b-dsp:5GB` в Ollama.
- **llama.cpp** установлен (готовые бинарники `b9049-bin-win-cpu`) в `E:\tools\llama.cpp`. `LLAMA_CPP_DIR` зарегистрирован.
- **Качество слабое** (r=4, 1093 примера, повторяющиеся YAML в датасете → модель копирует паттерн, не отвечает). Финал — на Radeon 9070 (12.05).
- **Шпаргалка**: `MemoryBank/specs/LLM_and_RAG/cheatsheet_qlora_train_metrics_2026-05-07.md` (метрики loss/lr/grad, грабли пройдены, конфиги для Phase B).
- **Сессия**: `MemoryBank/sessions/2026-05-07.md`. **Phase B TASK**: `MemoryBank/tasks/TASK_FINETUNE_phase_B_2026-05-12.md`.

## 2026-05-06 — TASK_RAG_09 ✅ Pipeline Generator (Агент 3)

- **3 pipeline'а** зарегистрированы: 1 в spectrum (`AllMaximaPipelineROCm` из header) + 2 в strategies (`antenna_processor_pipeline`, `farrow_pipeline` из `Doc/`).
- **doc_blocks**: 3 с concept=`pipeline`. **rag_dsp.pipelines** table: 3 типизированных строк. **Qdrant** `target_table='pipelines'`: 3 точки ✅ (DoD §4 PASS).
- **CLI**: `dsp-asst rag pipelines build [--repo X | --all] [--pipeline slug] [--dry-run] [--re-llm]`.
- **Источники**: 1) headers `<repo>/include/**/*pipeline*.hpp` (исключая infra: `Pipeline`/`PipelineBuilder`/`I*Pipeline`), 2) markdown `<repo>/Doc/*pipeline*.md` (extract `## Pipeline:` секций или весь файл).
- **Детерминированно**: composer_class, chain_classes (через `#include` lookup в `symbols` + INFRA-фильтр на `MemoryManager`/`GpuContext`/`IBackend`/...), chain_repos, ASCII flow (копия из Doc/ или autogen-stub).
- **LLM (Qwen3 8B, промпт 012, ≤50 строк)**: только title (с post-fix strip "Pipeline:" префикса) + when_to_use + synonyms_ru/en (8/8) + tags (5-10). Robust JSON parse + qwen-typo фиксы (synняonyms_en).
- **LLM cache**: `~/.dsp_asst_cache/llm_cpp_pipeline/{sha1}_{prompt_version}.json`. Идемпотентный re-run.
- **Ревью-фиксы (4)**: A) strip Pipeline: prefix в title, B) INFRA whitelist (16 классов: GpuContext/IBackend/MemoryManager/ProfilingFacade/...), C) расширенный PascalCase regex для chain mentions (ROCm/Op/Filter/Generator/... суффиксы), D) `--pipeline X` фильтр в CLI.
- **Артефакты**: `dsp_assistant/modes/pipeline_gen.py`, `MemoryBank/specs/LLM_and_RAG/prompts/012_pipeline.md`, `spectrum/.rag/pipelines.md`, `strategies/.rag/pipelines.md`. CLI группа `dsp-asst rag pipelines`.
- **DoD**: 4/5 PASS (тесты «по мере роста» — partial, robust JSON parse есть, AddStep<>() парсер не нужен — нет таких usages в проекте).

## 2026-05-06 — TASK_RAG_02.6 ✅ Python use-cases + pybind bindings

- **47 python_test_usecase карточек** в `DSP/.rag/use_cases/python__*.md` + 4 локальных `<repo>/.rag/use_cases/`.
- **5 cross_repo_pipeline** в `DSP/.rag/cross_repo/`.
- **31 python_binding doc_blocks** (regex-парсер pybind11 → 8 репо `<repo>/python/py_*.hpp`).
- **`pybind_bindings`**: 42 строки, 38/42 с `doc_block_id`, **25/31 cpp_symbol_id resolved** (через token-Jaccard на `#include <X>` пути для разных имён `Py<X>` ↔ real C++ class).
- **DDL миграция applied**: `pybind_bindings` +5 колонок (`py_module_name`, `py_class_name`, `py_method_name`, `pybind_file`, `doc_block_id` + FK).
- **CLI**: `dsp-asst rag python build` / `dsp-asst rag python bindings`.
- **LLM**: Qwen3 8B через ollama, prompt 013 (RU-fix), кэш `~/.dsp_asst_cache/llm_python_usecase/{sha1}_{prompt_version}_{prompt_hash}.json` с robust JSON parser (защита от qwen-glitch `synняonyms_en`).
- **Smoke retrieval**: «Python тест FIR фильтра ROCm» → наша карточка top-3 ✅; «pipeline signal_generators → spectrum» → cross_repo top-3 ✅.
- **Findings → создан TASK_remove_opencl_pybind_2026-05-06.md** (расширен на ВСЕ остатки OpenCL: pybind FirFilter/IirFilter, `cl_mem`/`clCreate*` API, OpenCL backend классы, `*_OpenCL_*` тесты).

Артефакты: `dsp_assistant/modes/{python_usecase_extractor,python_usecase_gen,pybind_extractor}.py`, `MemoryBank/specs/LLM_and_RAG/prompts/013_python_usecase.md`, `MemoryBank/specs/LLM_and_RAG/configs/postgres_init_pybind_extras.sql`, `scripts/rag_setup/{rollout_python_usecases,rollout_pybind_bindings,apply_pybind_extras}.py`.

## 2026-05-04 → 05 (ночная сессия)

- **doxytags core Phase 2 ✅ DONE** — закрыто **774 / 775** TODO-placeholder'ов в **52 файлах** `core/include/core/` (content-aware русские описания методов, не shotgun). Один оставшийся TODO в `gpu_manager.hpp:810` — реальный TODO разработчика («Реализовать ROCm discovery»), не doxytags. Доводит весь doxytags-проект (8 репо) до **9 / 9 завершённых модулей**.
- **Validators раскатка по 7 модулям** — создан **зонтичный** TASK `TASK_validators_all_modules_migration_2026-05-04.md` с inventory из реального скана: 16 / 66 файлов уже на `gpu_test_utils::*` (24%), 10-15 кандидатов на миграцию (~6-8 ч).
- (ранее в сессии) Phase B Python migration ✅, native pybind crashes ✅ (5 тестов), Validators port из GPUWorkLib ✅, Profiler v2 CI_RunSerial ✅.
