# Changelog — 2026-05

## 2026-05-14 — QLoRA Phase B Day-1 + RAG dataset_v6 ×2.5 🎯

**3 фазы Phase B закрыты на работе (Debian + RX 9070)** за ~4 часа:

- **Phase 1 Smoke matrix:** qwen3-8b eval **1.26** / coder-7b eval **1.18** (на dataset_v4, 1200 пар smoke) — Coder-7B обыграл general-8B на DSP-задачах → решение брать Qwen2.5-Coder-14B-Instruct в Phase 5
- **Phase 3 RAG → JSONL:** новый скрипт `finetune-env/collect_rag_v6.py` (310 строк, через `sudo -u postgres psql` peer-auth) → выгрузил **9159 train / 1490 val** из `rag_dsp.{symbols, doc_blocks, use_cases, test_params, pybind_bindings, files}` + 24 hardcoded anti-hallucination negatives. ×2.5 к dataset_v4, 12+ source-типов
- **Phase 4 Smoke #3:** coder-7b на dataset_v6 → **eval @ step 50 = 1.345 vs v4 1.44** (Δ −0.095) на **1310 samples (×18 надёжнее)** → гипотеза «RAG-обогащение даёт буст качества» **подтверждена**
- **Phase 3 v2 (расширение):** добавлены 7 builders в `collect_rag_v6.py` (БД: files+includes+cmake; FS: CLAUDE.md+Doc/*.md+.architecture+.claude/specs) + extended negatives (24→99). **Финал dataset_v6: 10204 train / 1660 val** (×2.06 к v4, +11% к v6 v1). Pool 9609 + v4 merge − 775 overlap → 11864 dedup pairs
- **Phase 6 dry-run inference compare:** на ckpt-50 v6 (smoke) против base coder-7b — **FT 2.8/6 vs base 0/6**. Q1 «HybridBackend = Bridge» точно (was Adapter+Factory у base), Q2 «ScopedHipEvent = RAII обёртка для hipEvent_t» точно (был общий). Q3/Q4 частично, Q5/Q6 ещё галлюцинируют — нужно >2000 шагов для запоминания test_params + anti-hall negatives.
- **Phase 6 infra:** `finetune-env/post_train.sh` (bash-аналог post_train.ps1, auto-clone+build llama.cpp, idempotent) — end-to-end PASS на coder-7b ckpt-50 → `qwen-coder-7b-dsp-v6` в Ollama. `Modelfile.template` фикс: repeat_penalty 1.05→1.20 + repeat_last_n 256 (для FT моделей).
- **Phase 5 infra:** `finetune-env/run_with_resume.sh` (shell-wrapper) + патч `train_simple.py --resume-from-checkpoint <path|auto>`. Critical для Phase 5 14B full train (8-12 ч) — HIP-race на 9070+ROCm 7.2 фундаментален, нужны save-steps=20 + auto-resume. Smoke 30 шагов PASS, retry-loop проверен.

**Технические открытия:**
- `bnb 0.49.2` 4-bit kernel под gfx1201: падает в `csrc/ops.hip:83` при `max_seq=1024+adamw_8bit`, safe Plan-B → `seq=256-512, lora_r=8, adamw_torch`
- HIP `is_nonzero/item` race в HF Trainer (NaN-check / LR scheduler) → `illegal address` на step 50-120 (фундаментальный ROCm 7.2 RDNA4 bug) → для Phase 5 нужен `--save-steps 20` + auto-resume
- `PYTORCH_HIP_ALLOC_CONF=expandable_segments` warning «not supported on this platform» (молча игнорируется)
- Любой доп. GPU-app (Telegram, браузер) во время train запрещён — compositor race
- Для большого датасета обязательно `--eval-split 0.02` (иначе 10 мин/eval × 6 evals = wallclock blow-up)

**Артефакты:**
- `MemoryBank/prompts/prompt_for_sister_phase_b_2026-05-14.md` (переписан под нас + сравнительные таблицы по 3 smoke)
- `MemoryBank/tasks/TASK_FINETUNE_phase_B_9070_2026-05-14.md` (DoD по 6 фазам)
- `MemoryBank/specs_Linux_Radion_9070/phase_b_models_analysis_2026-05-14.md` (VRAM budget)
- `MemoryBank/sessions/2026-05-14.md` (полный summary)
- `finetune-env/collect_rag_v6.py` (выгрузка RAG, переиспользуемая)
- `finetune-env/dataset_v6_{pool,dedup,train,val}.jsonl` (Alpaca формат)
- `finetune-env/output/smoke_9070_{qwen3-8b,coder7b,coder7b_v6}_2026-05-14/checkpoint-{50,100}/`

**Не сделано (Phase 2 вечером дома + Phase 5/6 завтра+):**
- Скачать Qwen2.5-Coder-14B-Instruct + Qwen3-14B + Coder-7B-Instruct (~70 GB) через интернет дома
- Full Phase B на Coder-14B (8-12 ч ночью, после привоза модели)
- `post_train.sh` (bash-аналог `post_train.ps1`) + Ollama deploy + `inference_compare.py` на 6 контрольных вопросах Q1-Q6

---

## 2026-05-13 — Большой день DSP-GPU на Debian 🏆

**4 крупных задачи закрыто за одну сессию** на рабочем Debian 13 + RX 9070 (gfx1201).

### 1. ROCm 7.2 devkit install ✅
- 76 .deb из offline-pack (Ubuntu noble) **не лезут** на Debian 13 (libc 2.41 vs noble 2.39, gcc-13 dep conflicts) — apt отказался ставить, система не тронута
- Решение: `sudo apt install hipcc hip-dev rocm-llvm hipfft-dev rocblas-dev rocsolver-dev rocprim-dev rocrand-dev hipblas-dev rocm-opencl-runtime` напрямую из подключённого `repo.radeon.com/rocm/apt/7.2/noble` (~3.7 GB скачано, без gcc-13 toolchain)
- Smoke 9/9: hipcc 1.1.1.70200, HIP 7.2.26015, AMD clang 22.0.0, /opt/rocm-7.2.0/lib/cmake/hip/hip-config.cmake ✅

### 2. Namespace migration acceptance — 26/26 PASS 🎉
- Acceptance script `scripts/debian_deploy/acceptance_namespace_migration.sh`: 7 build + 9 ctest + 8 Python imports + 2 Python integration
- 12 итераций (v1-v12), **8 групп фиксов**:
  - G1: spectrum_maxima_types.h include (legacy path → `dsp/spectrum/types/...`)
  - G3: `apt install hipblas + rocm-opencl-runtime`
  - G4: namespace shadow `dsp::X::` → `::dsp::X::` (34 файла через regex)
  - G5: fake-nested `namespace dsp::stats::snr_defaults` (внутри `dsp::stats`) → `namespace snr_defaults` (3 файла)
  - G6: `antenna_fft::` (global namespace, не `dsp::spectrum::`) + `drv_gpu_lib::OutputDestination` (5 файлов)
  - G7: tests legacy `signal_gen::` + `drv_gpu_lib::Heterodyne*` (6 файлов)
  - G8: HeterodyneROCmProfEvents namespace (тип ЖИВ в `dsp::heterodyne::`, sed-fix 2 файла)
  - GCC 14.2 bug: `using namespace ::drv_gpu_lib;` ВНУТРИ `namespace dsp::X {}` не работает — вынесено в global scope перед namespace в 11 файлах
- Итог: ~50 файлов исправлено, миграция дома (sed без сборки) **окончательно дочищена**
- Подробности: `tasks/TASK_namespace_migration_debian_acceptance_2026-05-12.md`

### 3. P1 — Python tests Phase B на RX 9070: 43 PASS / 6 FAIL ✅
- 50 t_*.py прогнаны через `/tmp/run_python_tests_v2.py` (regex по `Total: N passed, M failed, K skipped` из TestRunner output)
- **86% pass rate** на gfx1201: 43 PASS, 1 all-SKIP, 6 FAIL, 0 timeout/error-exit, общее время 42.8с
- 6 FAIL'ов — **НЕ инфраструктурные** (миграция чистая), а реальные numerical / API проблемы:
  - `heterodyne/t_heterodyne.py` 0/4 — полный провал (расследовать)
  - `radar/t_fm_correlator.py`, `radar/t_range_angle.py`, `spectrum/t_spectrum_maxima_finder_rocm.py` — numerical mismatch GPU vs reference
  - `spectrum/ai_pipeline/t_ai_pipeline.py` — API mismatch (`IirFilterROCm(ctx, b, a)` сигнатура устарела)
  - `linalg/t_capon.py` — детали
- Отложено в `TASK_python_migration_phase_B_FAILS_2026-05-04.md` (обновлён секцией 2026-05-13)
- ⚠️ Урок: первая попытка дала ложные «25 PASS / 25 SKIP» из-за case-insensitive `'SKIP' in out.upper()` — TestRunner печатает `0 skipped` в каждом отчёте, что триггерило false positive. **Парсить только через явный regex** на Total-строку.

### 4. V2 — validators linalg pilot ✅ (обнаружено inspection'ом)
- `gpu_test_utils::ScalarAbsError + throw on !v.passed` уже внедрён в 4 файла:
  - `test_cholesky_inverter_rocm.hpp` — 6 вхождений
  - `test_cross_backend_conversion.hpp` — 3
  - `test_capon_opencl_to_rocm.hpp` — 4
  - `test_capon_hip_opencl_to_rocm.hpp` — 2
- **0** ручных `if (err >= TOL) throw` в `linalg/tests/`
- Следующий шаг — rollout на остальные 7 модулей (отдельный TASK)

### Что осталось

- **O1** OpenCL legacy classes (5 файлов в spectrum + signal_generators) — отложено, рисковано без acceptance после каждого fix'а
- 6 FAIL P1 — расследование по одному
- F1 QLoRA Phase B — LLM/Dataset сторона
- RAG/Doc хвосты — исключены из плана сегодня

---

## 2026-05-13 — Continue: локальный bge-m3 embedding server ✅

Поднят локальный embedding-сервер для Continue VSCode @codebase. Заменён медленный встроенный transformers.js на FastAPI + onnxruntime + bge-m3 ONNX.

- **venv** `~/.continue/.venv` (Python 3.13, onnxruntime 1.26 / fastapi / uvicorn / tokenizers / numpy / pydantic) — pip из pypi (~30 MB), offline-pack cp312 wheels не подошли под системный py3.13.
- **embed_server.py** скопирован в `~/.continue/`, модель `bge-m3` грузится из `/home/alex/offline-debian-pack/1_models/bge-m3` (1024-dim, 8K ctx).
- **systemd user unit** `~/.config/systemd/user/embed.service` — active (running), Restart=on-failure, MemoryMax=4G, ExecStart через venv-python.
- **~/.continue/config.yaml** — добавлен `bge-m3 (local)` provider (`openai` совместимый, `apiBase: http://localhost:8765/v1`, role=embed); chat/autocomplete Qwen 3.6 не тронуты. Backup: `config.yaml.bak-2026-05-13`.
- **Smoke**: `/health` ok / dim=1024 / `/v1/embeddings` — multilingual EN/RU работает.

Минимальная цель «просто запустить локальный AI server» достигнута. RAG (Postgres + Qdrant) — отдельным таском позже.

Связано: `tasks/TASK_continue_embedding_setup.md` ✅ DONE.

---

## 2026-05-12 — Namespace migration 7/7 + offline-pack + Debian deploy scripts 🏆

**Эпическая Windows-сессия дома, 4 параллельных трека закрыты за один вечер.**

### 1. Namespace migration legacy → dsp::* (7/7 модулей, 30 коммитов)

Все 7 рабочих репо мигрированы на `dsp::<repo>::*` namespace по правилу `10-modules.md`:
- **spectrum** (fft_processor/filters/lch_farrow → dsp::spectrum) — 4 коммита, пилот
- **stats** (statistics/snr_estimator/gpu_sort/snr_defaults → dsp::stats + nested) — 3 коммита
- **strategies** (strategies → dsp::strategies + forward decls на migrated) — 3 коммита
- **signal_generators** (signal_gen → dsp::signal_generators) — 3 коммита
- **linalg** (vector_algebra/capon/matrix_ops → dsp::linalg) — 4 коммита
- **radar** (range_angle + fm_correlator drv_gpu_lib → dsp::radar) — 3 коммита
- **heterodyne** (drv_gpu_lib целиком → dsp::heterodyne) — 2 коммита, особый подход с `using namespace ::drv_gpu_lib;`

Каждый модуль прошёл 4 фазы: namespace+using+FQN replace → git mv include/X → include/dsp/X + #include rewrites → Doc/RAG content + test_params rename → structural cleanup (удалили OpenCL .cl, выпрямили src/X/src/ → src/X/, обновили CMake target_sources). **core НЕ трогали** — `drv_gpu_lib::*` остаётся в core (правило 10).

### 2. Offline-pack для рабочего Debian 📦

- **ROCm 7.2 devkit `.deb`** через WSL2 Ubuntu noble: **76 файлов / 3.7 GB** в `D:\offline-debian-pack\7_dop_files\lib_deb\`. Покрытие: hipcc + hip-runtime-amd + rocm-llvm/cmake + hipfft-dev + rocblas-dev + rocsolver-dev + rocprim-dev + rocrand-dev + транзитивные deps.
- **onnxruntime 1.20.* для cp312/manylinux_2_28** + numpy через `pip download`. `fastembed` пропущен — конфликт зависимостей.

### 3. Debian deploy scripts (`scripts/debian_deploy/`, 5 файлов)

- `install_rocm_devkit.sh` — apt install + 9-step smoke check (hipcc, hip-config.cmake, headers, rocminfo + gfx detect)
- `embed_server.py` — FastAPI + onnxruntime + bge-m3 ONNX, OpenAI-совместимый `/v1/embeddings`
- `embed.service` — systemd user unit
- `acceptance_namespace_migration.sh` — авто-чеклист для 7 мигрированных репо (build + ctest + Python smoke + integration)
- `README.md` — handoff на завтра

### 4. MemoryBank артефакты (6 TASK + 1 spec + 8 golden_set updates)

- `tasks/TASK_morning_handoff_2026-05-13.md` (новый, 227 строк, точка входа на завтра)
- `tasks/TASK_namespace_migration_debian_acceptance_2026-05-12.md` (новый, 205 строк, чеклист на Debian)
- `tasks/TASK_install_rocm_hip_sdk_debian_2026-05-12.md` (обновлён прогрессом offline-pack)
- `tasks/TASK_continue_embedding_setup.md` (обновлён прогрессом onnxruntime)
- `specs/namespace_migration_spectrum_plan_2026-05-12.md` (319 строк, рецепт миграции)
- `.future/TASK_namespace_migration_legacy_to_dsp.md` (статус 7/7, ждёт Debian acceptance)
- `specs/LLM_and_RAG/golden_set/qa_v{1,2}.jsonl` + 8 eval_reports — все FQN обновлены 7 раз

### Урок сессии

При cascade FQN replace через PowerShell — `\b` boundary срабатывает после `::`, создавая double-prefix `dsp::stats::dsp::stats::`. Решение: финальный fix-pass `-replace 'dsp::X::dsp::X::','dsp::X::'` после каждой группы. Также: `using namespace ::drv_gpu_lib;` в headers — антипаттерн, но единственный практичный способ для модулей с ADL-резолвом core классов внутри методов.

### Артефакты

- Сессия: `MemoryBank/sessions/2026-05-12.md` (полная)
- Завтрашний handoff: `MemoryBank/tasks/TASK_morning_handoff_2026-05-13.md`
- Скрипты: `scripts/debian_deploy/README.md`

## 2026-05-09 — RAG-инфра финал: CTX2 + ENRICH_TG + CLAUDE_C4 → dataset_v3 = 2221 пар ✅

- **CTX2 doxygen_test_parser** (БЛОКЕР снят): 219 hpp/8 репо → +645 INSERT +505 UPDATE в `rag_dsp.test_params`. Total 674→**1319** rows; ready_for_autotest 111→**983** (+783%).
- **ENRICH_TG** ✅ DoD вечер: 480/480 test_gen placeholder'ов обогащены реальным C++ через qwen3:8b (0 fail). Финальный `dataset_v3.jsonl` = **2221 пар** (DoD ≥2000 с запасом 11%).
- **CLAUDE_C4** ✅ DoD утра (параллельная сестра): 8/8 `<repo>/CLAUDE.md` C4-блоки + 8/8 `_RAG.md` `tags:` (66 inferred) + 8 pairs `claude_md_section` шаблона.
- **EV.E2 RAGAs + abstain** ✅: faithfulness(grounded)=0.8, hallucination=0.0; 4 метрики через ollama judge.
- **CTX4 mcp_atomic_tools** ✅: 4 MCP tools (`dsp_test_params`/`dsp_use_case`/`dsp_pipeline`/`dsp_doc_block`).
- **DS dataset_v3** ✅: 1093→2020 baseline через 5 шаблонов, потом +193 после CTX2 LEVEL 1, потом +8 после CLAUDE_C4 → **2221 final**.
- **Урок (memory):** долгие скрипты ⇒ heartbeat ПЕРЕД операцией + `flush=True` + `python -u` + парный `watch_*.py` (ENRICH_TG висел 30+с на template-методах, Alex не понимал считает или умерло).
- Артефакты: `sessions/2026-05-09.md`, `specs/rag_ctx2_implementation_review_2026-05-09.md`, `prompts/handoff_session_2026-05-09_morning.md` (rewritten).

## 2026-05-08 — Diagnostic утра: r-up vs dataset clean (парадоксальный результат) 🔬

- **3 эксперимента** на 2080 Ti: Phase A r=4 dirty (44 мин) → Diagnostic r=8 dirty (62 мин) → CLEAN r=8 247 (26 мин).
- **Парадокс**: CLEAN дал last-10 avg loss 0.815 (лучший), но **inference хуже всех** — «Rochester GPU» галлюцинация, потеря namespace `drv_gpu_lib::`, возврат зацикливания.
- **Гипотеза «датасет = bottleneck» (max-5/class) — ОПРОВЕРГНУТА**. Корень: catastrophic forgetting + потеря critical mass повторений факт-имён (1093 → 247 = -77% повторений `hipfft`/`drv_gpu_lib::`).
- **Методический вывод**: train_loss (даже last-N) ≠ inference quality на узкоспециальных датасетах. Только **inference compare** даёт честный judge.
- **Plan для Phase B (12.05) обновлён**: dirty 1093 + r=16 + bf16 baseline, расширение датасета через RAG до 2000+ как параллельный трек. **НЕ использовать clean-247**.
- Артефакты: `MemoryBank/sessions/2026-05-08.md`, `MemoryBank/specs/LLM_and_RAG/finetune_diagnostic_2026-05-08.md`, обновлён `MemoryBank/tasks/TASK_FINETUNE_phase_B_2026-05-12.md`.
- Скрипты: `run_full_qwen3_r8_clean.ps1`, `run_compare_3way.ps1`. Continue MCP проверен (✅ 200 OK).

## 2026-05-07 — QLoRA fine-tune Phase A (план A handoff'а — путь проторён) ✅

- **train_simple.py** (без trl, чистый transformers.Trainer) — фикс silent hang предыдущей ночи. План A зашёл: 411 шагов / 3 эпохи / 44 мин / loss 2.45→1.18.
- **End-to-end pipeline проторён**: `train → merge_lora → convert_hf_to_gguf → quantize Q4_K_M → ollama create`. Модель `qwen3-8b-dsp:5GB` в Ollama.
- **llama.cpp** установлен (готовые бинарники `b9049-bin-win-cpu`) в `E:\tools\llama.cpp`. `LLAMA_CPP_DIR` зарегистрирован.
- **Качество слабое** (r=4, 1093 примера, повторяющиеся YAML в датасете → модель копирует паттерн, не отвечает). Финал — на Radeon 9070 (12.05).
- **Шпаргалка**: `MemoryBank/specs/LLM_and_RAG/cheatsheet_qlora_train_metrics_2026-05-07.md` (метрики loss/lr/grad, грабли пройдены, конфиги для Phase B).
- **Сессия**: `MemoryBank/sessions/2026-05-07.md`. **Phase B TASK**: `MemoryBank/tasks/TASK_FINETUNE_phase_B_2026-05-12.md`.

## 2026-05-06 — TASK_RAG_09 ✅ Pipeline Generator (Агент 3)

- **3 pipeline'а** зарегистрированы: 1 в spectrum (`AllMaximaPipelineROCm` из header) + 2 в strategies (`antenna_processor_pipeline`, `farrow_pipeline` из `Doc/`).
- **doc_blocks**: 3 с concept=`pipeline`. **rag_dsp.pipelines** table: 3 типизированных строк. **Qdrant** `target_table='pipelines'`: 3 точки ✅ (DoD §4 PASS).
- **CLI**: `dsp-asst rag pipelines build [--repo X | --all] [--pipeline slug] [--dry-run] [--re-llm]`.
- **Источники**: 1) headers `<repo>/include/**/*pipeline*.hpp` (исключая infra: `Pipeline`/`PipelineBuilder`/`I*Pipeline`), 2) markdown `<repo>/Doc/*pipeline*.md` (extract `## Pipeline:` секций или весь файл).
- **Детерминированно**: composer_class, chain_classes (через `#include` lookup в `symbols` + INFRA-фильтр на `MemoryManager`/`GpuContext`/`IBackend`/...), chain_repos, ASCII flow (копия из Doc/ или autogen-stub).
- **LLM (Qwen3 8B, промпт 012, ≤50 строк)**: только title (с post-fix strip "Pipeline:" префикса) + when_to_use + synonyms_ru/en (8/8) + tags (5-10). Robust JSON parse + qwen-typo фиксы (synняonyms_en).
- **LLM cache**: `~/.dsp_asst_cache/llm_cpp_pipeline/{sha1}_{prompt_version}.json`. Идемпотентный re-run.
- **Ревью-фиксы (4)**: A) strip Pipeline: prefix в title, B) INFRA whitelist (16 классов: GpuContext/IBackend/MemoryManager/ProfilingFacade/...), C) расширенный PascalCase regex для chain mentions (ROCm/Op/Filter/Generator/... суффиксы), D) `--pipeline X` фильтр в CLI.
- **Артефакты**: `dsp_assistant/modes/pipeline_gen.py`, `MemoryBank/specs/LLM_and_RAG/prompts/012_pipeline.md`, `spectrum/.rag/pipelines.md`, `strategies/.rag/pipelines.md`. CLI группа `dsp-asst rag pipelines`.
- **DoD**: 4/5 PASS (тесты «по мере роста» — partial, robust JSON parse есть, AddStep<>() парсер не нужен — нет таких usages в проекте).

## 2026-05-06 — TASK_RAG_02.6 ✅ Python use-cases + pybind bindings

- **47 python_test_usecase карточек** в `DSP/.rag/use_cases/python__*.md` + 4 локальных `<repo>/.rag/use_cases/`.
- **5 cross_repo_pipeline** в `DSP/.rag/cross_repo/`.
- **31 python_binding doc_blocks** (regex-парсер pybind11 → 8 репо `<repo>/python/py_*.hpp`).
- **`pybind_bindings`**: 42 строки, 38/42 с `doc_block_id`, **25/31 cpp_symbol_id resolved** (через token-Jaccard на `#include <X>` пути для разных имён `Py<X>` ↔ real C++ class).
- **DDL миграция applied**: `pybind_bindings` +5 колонок (`py_module_name`, `py_class_name`, `py_method_name`, `pybind_file`, `doc_block_id` + FK).
- **CLI**: `dsp-asst rag python build` / `dsp-asst rag python bindings`.
- **LLM**: Qwen3 8B через ollama, prompt 013 (RU-fix), кэш `~/.dsp_asst_cache/llm_python_usecase/{sha1}_{prompt_version}_{prompt_hash}.json` с robust JSON parser (защита от qwen-glitch `synняonyms_en`).
- **Smoke retrieval**: «Python тест FIR фильтра ROCm» → наша карточка top-3 ✅; «pipeline signal_generators → spectrum» → cross_repo top-3 ✅.
- **Findings → создан TASK_remove_opencl_pybind_2026-05-06.md** (расширен на ВСЕ остатки OpenCL: pybind FirFilter/IirFilter, `cl_mem`/`clCreate*` API, OpenCL backend классы, `*_OpenCL_*` тесты).

Артефакты: `dsp_assistant/modes/{python_usecase_extractor,python_usecase_gen,pybind_extractor}.py`, `MemoryBank/specs/LLM_and_RAG/prompts/013_python_usecase.md`, `MemoryBank/specs/LLM_and_RAG/configs/postgres_init_pybind_extras.sql`, `scripts/rag_setup/{rollout_python_usecases,rollout_pybind_bindings,apply_pybind_extras}.py`.

## 2026-05-04 → 05 (ночная сессия)

- **doxytags core Phase 2 ✅ DONE** — закрыто **774 / 775** TODO-placeholder'ов в **52 файлах** `core/include/core/` (content-aware русские описания методов, не shotgun). Один оставшийся TODO в `gpu_manager.hpp:810` — реальный TODO разработчика («Реализовать ROCm discovery»), не doxytags. Доводит весь doxytags-проект (8 репо) до **9 / 9 завершённых модулей**.
- **Validators раскатка по 7 модулям** — создан **зонтичный** TASK `TASK_validators_all_modules_migration_2026-05-04.md` с inventory из реального скана: 16 / 66 файлов уже на `gpu_test_utils::*` (24%), 10-15 кандидатов на миграцию (~6-8 ч).
- (ранее в сессии) Phase B Python migration ✅, native pybind crashes ✅ (5 тестов), Validators port из GPUWorkLib ✅, Profiler v2 CI_RunSerial ✅.
