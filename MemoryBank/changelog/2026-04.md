# Changelog — 2026-04

- **2026-04-23** — KernelCache v2 верифицирован + закрыт scope «не сделано в Phase A-D». Core 113 PASS, spectrum/SG/linalg/strategies зелёные, radar 6 PASS + 1 pre-existing FAIL (RangeAngle T2, не KC). Disk cache coexist OK, VRAM без утечек. Tags `v0.3.0` на 6 репо + `v0.3.1` на core (polish) и signal_generators (Phase B2: 6 генераторов на `ResolveCacheDir` — unified cache path). Добавлены CLI `dsp-cache-list` в `core/tools/` + рекурсивный обход в `ListEntries`. `core/CMakeLists.txt` +`roc::rocblas` PUBLIC. `test_storage_services.hpp` — obsolete `TestKernelCacheService` физически удалён (394→272 LOC).
- **2026-04-23 (вечер)** — radar `RangeAngle [T2]` FIX + Profiler v2 Phase D CLOSED. Radar: исправлен знак `gen_ref_kernel` (up-chirp вместо conjugated) + тест `rx_single` → down-chirp + `range_m = r_frac × range_res_m × n_samples / nfft_range` (учёт zero-pad), commits `aba9529` + `29ac48b`, tag `v0.3.1`. T2: R_expected=75000m R_got=75000.69m err=0.69m PASS. Strategies Phase D Profiler v2: последние 2 файла (`strategies_profiling_benchmark.hpp` + `test_strategies_step_profiling.hpp`) мигрированы с deprecated `GPUProfiler` на `ProfilingFacade` (Enable/SetGpuInfo/BatchRecord/WaitEmpty/ExportJsonAndMarkdown), commit `f30ec5f`, tag strategies `v0.3.1`. 4 PASS / 0 FAIL.
