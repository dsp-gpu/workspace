<!-- type:meta_rules_index repo:dsp_gpu source:MemoryBank/.claude/rules/ -->

# Rules Index — DSP-GPU

Snapshot 17 канонических правил из `MemoryBank/.claude/rules/`. Полный текст — в исходных файлах.

## Список правил

| Файл | Заголовок | Краткое описание |
|------|-----------|------------------|
| `00-new-task-workflow.md` | 00 — Workflow при новой задаче | Правило без `paths:` — **грузится всегда**. Короткое, чтобы помнить в каждой сессии. |
| `01-user-profile.md` | 01 — User & Assistant Profile |  |
| `02-workflow.md` | 02 — Workflow (сессия: начало → работа → конец) |  |
| `03-worktree-safety.md` | 03 — 🚨 Worktree Safety (нарушать нельзя) | **Это правило выше всех остальных.** |
| `04-testing-python.md` | 04 — 🚫 Python Testing (pytest FORBIDDEN НАВСЕГДА) | ⚠️ **Правило действует НАВСЕГДА** — и после завершения DSP-GPU, во всех будущих проектах Alex. |
| `05-architecture-ref03.md` | 05 — Единая архитектура GPU-операций (Ref03) | Все модули DSP-GPU строятся по единой **6-слойной модели**. |
| `06-profiling.md` | 06 — Профилирование GPU (ProfilingFacade v2) | ⚠️ Старый `GPUProfiler` — `@deprecated` (до Phase D). |
| `07-console-output.md` | 07 — Консольный вывод (ConsoleOutput — одна точка на все GPU) | ⚠️ До 10 GPU работают параллельно. Без единой точки вывод перемешается. |
| `08-logging.md` | 08 — Логирование (Logger + plog, per-GPU) | Backend: **plog**. Один логгер на GPU, файлы раздельные. |
| `09-rocm-only.md` | 09 — 🔴 ROCm ONLY | **DSP-GPU — ROCm-only проект.** Никакого clFFT/cuFFT. |
| `10-modules.md` | 10 — Модули DSP-GPU (10 репозиториев) | DSP-GPU разбит на **10 независимых git-репозиториев** под org `github.com/dsp-gpu/`. |
| `11-python-bindings.md` | 11 — Python Bindings (pybind11) | Все значимые C++ модули получают Python API через pybind11. |
| `12-cmake-build.md` | 12 — CMake Build (Debian + ROCm) | ⚠️ **БЕЗ явного OK от Alex — CMake НЕ ПРАВИМ.** CMake — скелет 10 репо. |
| `13-optimization-docs.md` | 13 — Оптимизация HIP/ROCm и дополнительная документация |  |
| `14-cpp-style.md` | 14 — C++ Style & Design (ООП / SOLID / GRASP / GoF) | Стиль программирования DSP-GPU. Применяется ко всем C++ файлам. |
| `15-cpp-testing.md` | 15 — C++ Testing (тот же ООП/SOLID/GRASP стиль) | C++ тесты — **обычные классы** в том же стиле что основной код. |
| `16-github-sync.md` | 16 — GitHub Sync: «запушь всё» / «обнови репо» | **Приоритет**: выше `02-workflow.md:38` («не пушить без OK») — триггерная фраза сама И ЕСТЬ запрос, но всё равно нужен к |

## Полный snapshot заголовков и preview

### 00 — Workflow при новой задаче (`00-new-task-workflow.md`)

> Правило без `paths:` — **грузится всегда**. Короткое, чтобы помнить в каждой сессии.

Подзаголовки:
- Последовательность (строго)
- Правило «не кодить сразу»
- Исключения (когда пропускаем шаги)
- Что обязательно ВСЕГДА

### 01 — User & Assistant Profile (`01-user-profile.md`)

Подзаголовки:
- 👤 Alex (пользователь)
  - Реагирует болезненно на
- 🤖 Кодо (AI Assistant)
- 🎯 Стиль работы
- Стиль программирования (коротко)

### 02 — Workflow (сессия: начало → работа → конец) (`02-workflow.md`)

Подзаголовки:
- 📖 В начале сессии
- 💻 Во время работы
- 📝 В конце сессии
- 🎯 Приоритеты (в порядке убывания)
- 🚫 Запреты процесса
- 🗣️ Команды Alex

### 03 — 🚨 Worktree Safety (нарушать нельзя) (`03-worktree-safety.md`)

> **Это правило выше всех остальных.**

Подзаголовки:
- Проблема
- Правило
- Как проверить перед записью
- Агенты и синьоры
- Правильные места для результатов

### 04 — 🚫 Python Testing (pytest FORBIDDEN НАВСЕГДА) (`04-testing-python.md`)

> ⚠️ **Правило действует НАВСЕГДА** — и после завершения DSP-GPU, во всех будущих проектах Alex.

Подзаголовки:
- Наша библиотека тестирования (TestRunner)
  - Использование
  - Обнаружение тестов (для class-based варианта A)
  - Обработка исключений
- ЗАПРЕЩЕНО писать где-либо
- Запуск тестов
- Размещение тестов
- Python интерпретатор

### 05 — Единая архитектура GPU-операций (Ref03) (`05-architecture-ref03.md`)

> Все модули DSP-GPU строятся по единой **6-слойной модели**.

Подзаголовки:
- Слои (коротко)
- Ключевые правила
- Структура модуля
- Вызов тестов из main
- Ссылки

### 06 — Профилирование GPU (ProfilingFacade v2) (`06-profiling.md`)

> ⚠️ Старый `GPUProfiler` — `@deprecated` (до Phase D).

Подзаголовки:
- Единая точка
- Обязательный порядок
- Режимы записи
- Паттерн CollectOrRelease
- 🚫 Запрещено
- Timing source (Q7)
  - Hardware counters (Q7.F)
- Параллельный прогон тестов

### 07 — Консольный вывод (ConsoleOutput — одна точка на все GPU) (`07-console-output.md`)

> ⚠️ До 10 GPU работают параллельно. Без единой точки вывод перемешается.

Подзаголовки:
- Дизайн (AsyncServiceBase)
- API (все неблокирующие)
- Формат вывода
- Включение / выключение
- 🚫 ЗАПРЕЩЕНО
- Почему критично
- configGPU.json (per-GPU флаги)

### 08 — Логирование (Logger + plog, per-GPU) (`08-logging.md`)

> Backend: **plog**. Один логгер на GPU, файлы раздельные.

Подзаголовки:
- Заголовок
- Получение логгера
- Макросы (рекомендуемый способ)
- Уровни
- Размещение логов
- Разница с ConsoleOutput
- Factory (для тестов / production подмены)
- 🚫 Запрещено

### 09 — 🔴 ROCm ONLY (`09-rocm-only.md`)

> **DSP-GPU — ROCm-only проект.** Никакого clFFT/cuFFT.

Подзаголовки:
- Разрешённые библиотеки
- 🚫 Запрещено
- CMake (Debian: `find_package` — lowercase!)
- Компилятор
- События для профилирования
- Internal / External контекст
- Legacy GPUWorkLib

### 10 — Модули DSP-GPU (10 репозиториев) (`10-modules.md`)

> DSP-GPU разбит на **10 независимых git-репозиториев** под org `github.com/dsp-gpu/`.

Подзаголовки:
- Список репозиториев
- Каноничные имена
- Структура одного репо
- Граф зависимостей
- Namespace
- Статус миграции (legacy → DSP-GPU)

### 11 — Python Bindings (pybind11) (`11-python-bindings.md`)

> Все значимые C++ модули получают Python API через pybind11.

Подзаголовки:
- Политика
  - ✅ Требуется Python API
  - ❌ НЕ требуется
- Размещение
- Auto-deploy в DSP/Python/libs/ (CMake POST_BUILD)
- Миграция с legacy
- Требования к API
- Импорт из Python

### 12 — CMake Build (Debian + ROCm) (`12-cmake-build.md`)

> ⚠️ **БЕЗ явного OK от Alex — CMake НЕ ПРАВИМ.** CMake — скелет 10 репо.

Подзаголовки:
- 🚫 Запрещено без согласования (АБСОЛЮТ)
- ✅ Разрешено (очевидные правки)
- Пресеты
- `find_package` — lowercase! (Linux case-sensitive)
- Опции
- Быстрые проверки
- Зависимости между репо (коротко)

### 13 — Оптимизация HIP/ROCm и дополнительная документация (`13-optimization-docs.md`)

Подзаголовки:
- 📖 Гайды по оптимизации (источник истины)
- 🎨 Диаграммы
- 🏗️ Архитектура
- 💡 Примеры
- 📚 Doxygen
- 🗂️ Где что лежит
- Чеклист для нового kernel
- Внешние ресурсы (через WebFetch / Context7)

### 14 — C++ Style & Design (ООП / SOLID / GRASP / GoF) (`14-cpp-style.md`)

> Стиль программирования DSP-GPU. Применяется ко всем C++ файлам.

Подзаголовки:
- 🎯 Базовые принципы
- 🚫 Не плодить новые сущности
- 📁 Файловая организация
- 🔤 Naming & Style
- 🧱 Типовые контракты
- ⚠️ Запреты в коде

### 15 — C++ Testing (тот же ООП/SOLID/GRASP стиль) (`15-cpp-testing.md`)

> C++ тесты — **обычные классы** в том же стиле что основной код.

Подзаголовки:
- Дизайн
  - GRASP / GoF в тестах
- Размещение
- Ключевые точки
  - `all_test.hpp` (список + включение/отключение)
  - Главный `src/main.cpp` (корневой)
  - Полный шаблон `test_*.hpp`
- Правила хороших тестов

### 16 — GitHub Sync: «запушь всё» / «обнови репо» (`16-github-sync.md`)

> **Приоритет**: выше `02-workflow.md:38` («не пушить без OK») — триггерная фраза сама И ЕСТЬ запрос, но всё равно нужен к

Подзаголовки:
- Триггерные фразы Alex
- 🚨 Алгоритм (жёстко)
  - Шаг 1 — переспросить (один раз, коротко)
  - Шаг 2 — дождаться ЯВНОГО «да» / «делай» / «ок»
  - Шаг 3 — выполнить по ВСЕМ репо
  - Шаг 4 — верификация (ОБЯЗАТЕЛЬНО)
  - Шаг 5 — отчёт таблицей
- 🚫 Запрещено

