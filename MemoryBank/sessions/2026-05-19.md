# Сессия 2026-05-19 — P1 Python migration Phase B → ✅ DONE

**Платформа**: Debian Linux + ROCm 7.2.26015 + AMD Radeon RX 9070 (gfx1201)

## 🎯 Итог дня

**P1 Python migration Phase B завершён**: 50/54 PASS (92.6%), 1 FAIL (off-scope), 3 SKIP (env-related).

Asserts: **314 PASS / 0 FAIL / 15 SKIP** (было 13.05: 43 PASS / 6 FAIL).

## ✅ Сделано

### 1. Pull всех 10 репо (после github выкаченных коммитов 14-18.05)
- workspace, core, spectrum, stats, signal_generators, heterodyne, linalg, radar, strategies, DSP — все `Already up to date` после ручного pull (network тормозил, скрипт `acceptance_namespace_migration.sh --only-build` зависал на pull-фазе → собирали напрямую через `cmake --preset debian-local-dev`).

### 2. B0 sanity-чек артефактов Phase A
- `DSP/Python/libs/.gitkeep` ✅, shim `gpuworklib.py` отсутствует ✅, `gpu_loader.py` → `libs/` ✅
- CMake `POST_BUILD` patch во всех 8 модулях ✅

### 3. B1 чистая сборка 8 репо
- Скрипт `/tmp/build_all.sh` — порядок зависимостей (core → spectrum/stats/signal_generators/heterodyne/linalg → strategies → radar)
- Результат: **8/8 OK, 0 fail**
- 8 `.so` в `DSP/Python/libs/` обновлены ✅

### 4. B3 прогон 54 t_*.py
- **Новый универсальный runner**: `DSP/Python/run_all_tests.py` (постоянный, не в /tmp)
- 4-форматный парсер вывода тестов:
  1. `Total: N passed, M failed, K skipped` (TestRunner)
  2. `Results: N/M passed` (standalone t_*.py)
  3. `VERDICT: PASSED/FAILED` (single-verdict сравнительные тесты)
  4. `[SKIP] Suite init failed` (suite-level skip)
- Финальный итог: **50/54 PASS, 1 FAIL, 3 SKIP**

### 5. B4 разбор FAIL'ов — точечные правки
- **`t_heterodyne_comparison.py`** — root cause найден через глубокое ревью кода:
  - Симптом: GPU vs theory off by **66 kHz** константно на всех 5 антеннах
  - Арифметика: 66 kHz = ровно 44 bins (fbin=1500 Hz) = 22 μs (μ=3 GHz/s)
  - Причина: **строка 161** — `ref_single = np.exp(1j * phase_ref)` была без conj, тогда как kernel ожидает `ref = conj(s_tx)` (см. `heterodyne_kernels_rocm.hpp:54`)
  - Kernel вычисляет `conj(rx * ref) = conj(s_rx · conj(s_tx)) = conj(s_rx) · s_tx` → +μ·τ. Без conj в ref получалось `conj(rx · s_tx)` → фаза с противоположным знаком → spectrum смещён
  - **Фикс**: `np.exp(1j * phase_ref)` → `np.exp(-1j * phase_ref)` (одна строка)
  - Результат: max df 66300 → **300 Hz** (улучшение ×220), VERDICT PASSED ✅
- **4 sub-репо t_*.py** (spectrum/linalg/radar/strategies python/) — IMPORT_FAIL → PASS через добавление `DSP/Python/` в sys.path + `GPULoader.setup_path()`

### 6. Off-scope (не делаем)
- **`t_ai_filter_pipeline.py`** — IMPORT_FAIL: требует `api_keys.json` (ключи LLM AI-сервисов для AI-генерации фильтров). Секреты в git хранить нельзя, тест — off-scope DSP-GPU. Документирован в `TASK_python_migration_phase_B_FAILS`.

## 🔧 Изменённые файлы (несохранённые на момент сессии)

- `spectrum/python/t_cpu_fft.py` — sys.path fix
- `linalg/python/t_linalg.py` — sys.path fix
- `radar/python/t_radar.py` — sys.path fix
- `strategies/python/t_strategies.py` — sys.path fix
- `DSP/Python/heterodyne/t_heterodyne_comparison.py` — `+1j` → `-1j` в ref
- `DSP/Python/run_all_tests.py` — NEW (универсальный 4-форматный runner)

## 📊 Сравнение прогонов

| Дата | PASS | FAIL | SKIP | UNKNOWN | Asserts P/F/S |
|------|-----:|-----:|-----:|--------:|---------------|
| 2026-05-13 | 43 | 6 | 1 | — | — / — / — |
| 2026-05-18 пер. | 19 | 0 | 1 | 29 (парсер) | 158 / 0 / 13 |
| 2026-05-19 (фин.) | **50** | **0** | **3** | **0** | **314 / 0 / 15** |

## 📌 Уроки

1. **/tmp скрипты исчезают** — записан в memory `feedback_no_scripts_in_tmp.md`. Скрипт прогона перенесён в `DSP/Python/run_all_tests.py`.
2. **Двойной conj в heterodyne** — субтильность: kernel ждёт `ref = conj(s_tx)`, делает `conj(rx*ref)`. Если передать `ref = s_tx` (без conj), фаза перевернётся, spectrum съедет.
3. **github SSH тормозил** сегодня — несколько раз приходилось переключаться на точечные команды (по одному репо).
4. **Acceptance скрипт `--only-build` зависал на pull-фазе** — нужна опция `--no-pull` (TODO).

## Связанные

- TASK: [`tasks/TASK_python_migration_phase_B_debian_2026-05-03.md`](../tasks/TASK_python_migration_phase_B_debian_2026-05-03.md) → ✅ DONE 2026-05-19
- TASK FAILS: [`tasks/TASK_python_migration_phase_B_FAILS_2026-05-04.md`](../tasks/TASK_python_migration_phase_B_FAILS_2026-05-04.md) → обновлено: 5 из 6 FAIL'ов 13.05 закрыты, остался 1 off-scope (ai_filter_pipeline)
