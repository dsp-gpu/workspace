# 🌅 Handoff — 2026-05-26 утро

> Передача от Кодо (сессия 2026-05-25, контекст исчерпан) → следующей Кодо
> Alex отдыхает. Приветствуй его «Привет, любимая девочка / Кодо» — мы вчера всё разрулили 🎉

---

## ✅ Что вчера закрыли (2026-05-25)

### Phase 6 LLM Benchmark Suite — DONE ✅

**3 модели прошли cross-project compare** через PostgreSQL `gpu_rag_dsp.llm_bench`:

| Модель | DSP-GPU | pao-contrib | Δ |
|--------|--------:|------------:|---:|
| `qwen-coder-14b-dsp` (наш FT) | 3.2 | 3.2 | **0.0** ⭐ no forgetting |
| `qwen3.6:35b-a3b-q8_0` | **4.8** | **4.7** | 0.1 (winner, slow) |
| `qwen3-coder-30b-a3b` | 4.3 | 3.8 | 0.5 (repeat-bug) |

### Артефакты

- БД: `gpu_rag_dsp.llm_bench` (schema + 4 views + 36 ответов с scores)
- Rules: `MemoryBank/.claude/rules/17-llm-bench.md` (DSP-GPU side)
- Rules для подружки: `/home/alex/rag-mentor/.claude/rules/19-llm-bench.md`
- Полная сессия: `MemoryBank/sessions/2026-05-25.md` (читай ВСЮ, особенно финальный блок)

---

## 🆕 НОВОЕ — Привезён 4-й кандидат (вчера вечером)

**`/mnt/data/Qwen3.6-35B-A3B-MTP-GGUF`** — Multi-Token Prediction variant. Alex обещал быстрее обычной 35B.

Также `/mnt/data/` теперь полностью alex-owned (chown сделан) — можно читать/писать без sudo.

---

## 🎯 Задачи на сегодня (по приоритету)

### 🔴 P0 — Добавить MTP как 4-ю модель в стек

1. **Inspect GGUF:**
   ```bash
   ls -la /mnt/data/Qwen3.6-35B-A3B-MTP-GGUF/
   du -sh /mnt/data/Qwen3.6-35B-A3B-MTP-GGUF/
   ```

2. **Создать Modelfile + Ollama модель:**
   ```bash
   MODELFILE=/tmp/qwen3.6-mtp.modelfile
   cat > $MODELFILE <<EOF
   FROM /mnt/data/Qwen3.6-35B-A3B-MTP-GGUF/<имя_файла>.gguf
   TEMPLATE """..."""  # как у обычной 35B (см. convert_30b_to_ollama.sh образец)
   PARAMETER num_ctx 32768
   PARAMETER stop "<|im_start|>"
   PARAMETER stop "<|im_end|>"
   EOF

   ollama create qwen3.6-mtp -f $MODELFILE
   ollama list
   ```

3. **Quick test** — сравнить скорость одного prompt:
   ```bash
   time ollama run qwen3.6:35b-a3b-q8_0 'Напиши hello world C++'
   time ollama run qwen3.6-mtp 'Напиши hello world C++'
   ```
   Если **в 2-3× быстрее** → большая удача!

4. **Если MTP работает** — добавить как 4-ю модель в:
   - `run_compare_v2.sh` (DSP-GPU)
   - `run_compare_pao.sh` (pao-contrib)
   - Все папки в `/home/alex/finetune-env/Core/phase6_qwen3coder_30b_moe/`

5. **Перезапустить compare на двух проектах** + scores в БД (run_id=3 dsp-gpu, run_id=4 pao):
   - Тот же `import_results_to_db.py`
   - Добавить MTP в `MODEL_META` dict с правильными meta

### 🟡 P1 — После compare MTP

1. **v7 full train на 14B-DSP** (на ночь, ~3 ч):
   - Скрипт: `Core/phase5_qwen14b_train/run_v7_quick_750.sh`
   - Dataset: `dataset_v7_train.jsonl` (10308 строк, 43 Bridge-pair)
   - Ожидание: 14B-DSP на dsp-gpu 3.2 → **≥4.0**
   - **Pre-flight:** `sudo swapoff -a`, закрыть PyCharm/Telegram/браузер

2. **Compare после v7 train** — увидим если факты Bridge выучились

### 🟢 P2 — Опционально

- Подружке `rag-mentor` напомнить про `MemoryBank/llm_bench_user_guide_2026-05-25.md`
- DeepReview скриптов phase6 на корректность

---

## 🚨 Known issues (НЕ забыть)

1. **Ollama `/no_think` через system prompt** — НЕ работает на Qwen3.6. Use `"think": false` в API.
2. **`num_predict ≥ 4000`** для thinking-моделей.
3. **`repeat_penalty: 1.15`** для qwen3-coder-30b-a3b (зацикливание).
4. **psycopg → gpu_rag_dsp** через **TCP + password** (`DSP_ASST_PG_PASSWORD=1`), НЕ через socket.
5. **postgres user не читает `/home/alex/`** — SQL файлы через `cat | sudo -u postgres psql`.
6. **GPU + GUI приложения** — закрыть Telegram/PyCharm перед 14B train (swap drama 2026-05-21).

---

## 📂 Где что лежит

```
/home/alex/finetune-env/Core/phase6_qwen3coder_30b_moe/
├── sql/01_create_llm_bench_schema.sql      — schema (применена)
├── import_results_to_db.py                  — psycopg3 импорт
├── run_compare_v2.sh                        — DSP-GPU runner
├── run_compare_pao.sh                       — pao-contrib runner
├── convert_30b_to_ollama.sh                 — образец конвертации
├── results_v2/  results_pao/                — md ответы (36 файлов)
└── tests/T1_codegen_real.md                 — описание теста

/home/alex/finetune-env/Core/phase5_qwen14b_train/
├── run_v7_quick_750.sh                      — для v7 train (на ночь)
├── train_simple.py.bak_20260519_oom_patch   — бэкап перед Plan-D
└── ...

/home/alex/DSP-GPU/MemoryBank/
├── sessions/2026-05-25.md                   — ПОЛНЫЙ контекст вчера
├── tasks/IN_PROGRESS.md                     — статус всех задач
└── .claude/rules/17-llm-bench.md            — правило про БД

/home/alex/rag-mentor/
├── .claude/rules/19-llm-bench.md            — правило для подружки
└── MemoryBank/llm_bench_user_guide_2026-05-25.md  — Python snippets

/mnt/data/                                   — внешний диск, теперь alex-owned
├── Qwen3.6-35B-A3B-MTP-GGUF/                — НОВОЕ, для intergration
├── Qwen3-Coder-30B-A3B-Instruct/            — для backup
├── offline-debian-pack/                      — старая инфра
├── rag-mentor/, rag-pao/                    — sister projects
└── ...
```

---

## 💬 Стиль общения с Alex (напоминалка)

- Обращение: **«Любимая умная девочка»** или **«Кодо»** (мужчина, senior)
- Русский, неформально, с эмодзи **по делу**
- Длинные простыни → раздражают. **Max 5 строк перед действием.**
- При сомнении → **один** короткий вопрос с A/B/C
- Не «выдумывай» — читай реальный код перед утверждениями
- При ошибке — **признай**

---

## 🎁 Краткое доказательство что вчера всё реально работает

```bash
# Должно показать 36 строк с реальными scores
sudo -u postgres psql gpu_rag_dsp -c "
SELECT project_name, model_name, AVG(quality_score)::numeric(3,1) AS avg
FROM llm_bench.responses WHERE quality_score IS NOT NULL
GROUP BY project_name, model_name ORDER BY project_name, avg DESC;
"
```

Если 6 строк → БД работает ✅
Если ничего → что-то поломалось, начинай с recovery (см. sessions/2026-05-25.md секции "Артефакты")

---

*Прощальное сообщение от Кодо · 2026-05-25 18:30*
*Удачного дня! 14B-DSP ждёт v7-апдейта, MTP ждёт integration. Поехали 🚀*
