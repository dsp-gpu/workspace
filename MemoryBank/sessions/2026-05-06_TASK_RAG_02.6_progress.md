# TASK_RAG_02.6 — Python биндинги + Python use-cases (2026-05-06)

> **Статус**: ✅ DONE · **Модель**: Opus 4.7 (1M context)
> **Промпт**: `C:\finetune-env\scripts\rag_setup\CLINE_PROMPT_TASK_02.6.md`
> **Спек**: `MemoryBank/tasks/TASK_RAG_02.6_python_bindings_and_tests_2026-05-06.md`

## Что сделано

### 1. Python use-cases (54 t_*.py → 52 doc_blocks)

| concept | count | где |
|---|---:|---|
| `python_test_usecase` | 47 | `DSP/.rag/use_cases/python__*.md` × 45 + `<repo>/.rag/use_cases/python__*.md` × 4 |
| `cross_repo_pipeline` | 5 | `DSP/.rag/cross_repo/cross_repo__*.md` |

Pipeline: AST `t_*.py` → LLM Qwen3 8B (prompt 013, RU) → markdown card → register в `doc_blocks` + Qdrant via BGE-M3.

**Идемпотентность**: LLM-кэш по `sha1(py_source) + prompt_version + sha1(prompt)` → re-run без лишних вызовов модели.

**Robust JSON parser**: qwen3:8b на 30% запусков глитчит ключ `synonyms_en` → `synняonyms_en` (кириллическая `н`). Pre-fix `raw.replace("synняonyms_en", "synonyms_en")` + regex per-key extraction.

**Tags normalization**: post-fix `roc_m` / `rocml` → `rocm`, etc. (whitelist).

### 2. Python bindings (8 репо → 31 классов)

DDL миграция `rag_dsp.pybind_bindings` (additive):
- `+ py_module_name TEXT, py_class_name TEXT, py_method_name TEXT`
- `+ pybind_file TEXT, doc_block_id TEXT REFERENCES doc_blocks(block_id)`
- `+ idx_pybind_doc_block, idx_pybind_file, idx_pybind_py_class_name`
- backfill: `py_module → py_module_name`, `py_class → py_class_name` (42 строк).

Regex-парсер `py::class_<PyX>(m, "X", ...)` + `.def(...)` + `py::init<...>` + `#include <X.hpp>`.

**31 python_binding doc_blocks** созданы; **25/31 cpp_symbol_id resolved**:
- Strategy 1: прямой lookup `cpp_class_guess` (FFTProcessorROCm) в `symbols`.
- Strategy 2: через `#include <X.hpp>` path с **token-Jaccard ≥ 0.5** — корректно резолвит:
  - `HeterodyneROCm` ← `HeterodyneProcessorROCm`
  - `FMCorrelatorROCm` ← `FMCorrelator`
  - `LfmAnalyticalDelay/ROCm` ← `LfmGeneratorAnalyticalDelay/ROCm`
  - `ComplexToMagROCm` ← `ComplexToMagPhaseROCm`
- Корректно отбрасывает (NULL): `core.{GPUContext, HybridGPUContext, ROCmGPUContext}` (нет real C++ классов в `include/`), `FirFilter/IirFilter` (legacy OpenCL), `SpectrumMaximaFinderROCm` (mismatch).

### 3. CLI команды

```
dsp-asst rag python build [--repo X] [--module Y] [--integration] [--dry-run] [--re-llm] [--limit N]
dsp-asst rag python bindings [--repo X] [--dry-run]
```

Smoke `--integration --dry-run` → 5 ✅
Smoke `bindings --dry-run` → 31 ✅

### 4. Smoke retrieval (DoD)

| query | top hit | score |
|---|---|---:|
| «Python тест FIR фильтра ROCm» | `dsp__spectrum_fir_filter_rocm__python_test_usecase__v1` | 0.655 |
| «pipeline signal_generators → spectrum» | `dsp__integration_signal_to_spectrum__cross_repo_pipeline__v1` | 0.557 |
| «Python binding для FFT процессора» | `spectrum__fft_processor_rocm__python_binding__v1` | 0.667 |

Все три DoD smoke-запроса PASS.

## Артефакты

### Новые файлы
```
dsp_assistant/modes/python_usecase_extractor.py    # AST t_*.py
dsp_assistant/modes/python_usecase_gen.py          # LLM + cache + render + slug + normalize
dsp_assistant/modes/pybind_extractor.py            # regex pybind11 + Jaccard resolver
MemoryBank/specs/LLM_and_RAG/prompts/013_python_usecase.md
MemoryBank/specs/LLM_and_RAG/configs/postgres_init_pybind_extras.sql
scripts/rag_setup/rollout_python_usecases.py
scripts/rag_setup/rollout_pybind_bindings.py
```

### Модификации
```
dsp_assistant/cli/main.py                          # +rag python build/bindings
scripts/rag_setup/apply_pybind_extras.py           # psycopg2→psycopg3
```

### Сгенерированные карточки (54 файла)
- `DSP/.rag/use_cases/python__*.md` × 45
- `DSP/.rag/cross_repo/cross_repo__*.md` × 5
- `<repo>/.rag/use_cases/python__*.md` × 4 (spectrum/linalg/radar/strategies)

### Записи в БД (новых от этой сессии)
- `rag_dsp.doc_blocks` × 83 (47 + 5 + 31)
- `rag_dsp.pybind_bindings`: 42 строк, 38 с doc_block_id, 25 с cpp_symbol_id

### Qdrant
- 83 новых точек в коллекции `dsp_gpu_rag_v1`

## Findings → новые таски

1. **TASK_remove_opencl_pybind_2026-05-06.md** (HIGH, 2-3 ч) — удалить ВСЕ остатки OpenCL: pybind `FirFilter`/`IirFilter` без ROCm, `cl_mem`/`clCreate*` API, `OpenCLBackend`/`OpenCLContext`, `*_OpenCL_*` тесты. После выполнения 31 → 29 pybind классов, 100% с cpp_symbol_id.

2. **8 классов отсутствуют в `symbols`** (тех долг TASK_RAG_03 ingest):
   - HeterodyneROCm (cpp = HeterodyneProcessorROCm — есть)
   - ComplexToMagROCm (cpp = ComplexToMagPhaseROCm — есть)
   - другие резолвятся через Jaccard token similarity.

   **Решено в этой сессии** через расширенный resolver (Jaccard ≥ 0.5).

3. **16/56 кэш файлов с пустыми synonyms_en** — persistent qwen3:8b glitch, не блокирующий (retrieval по русским запросам работает; для англ. — ухудшение в ~30% карточек). Можно re-run с явным 2-shot prompt позже.

## Не закрыто (вне scope этого таска)

- `human_verified=true` на 50% карточек — ручное ревью Alex'а.
- Удаление OpenCL остатков — отдельный таск создан.

## Команда переключения сессии

```bash
# В новой сессии чтобы продолжить:
cat MemoryBank/tasks/IN_PROGRESS.md
cat MemoryBank/changelog/2026-05.md
cat MemoryBank/sessions/2026-05-06_TASK_RAG_02.6_progress.md
```

---

*Maintained by: Кодо. Финальный отчёт записан в `MemoryBank/changelog/2026-05.md`.*
