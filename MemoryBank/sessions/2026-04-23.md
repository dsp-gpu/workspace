# Сессия 2026-04-23 — KernelCache v2: build + test + polish

> **Платформа**: Debian + AMD Radeon RX 9070 + ROCm 7.2.0
> **Старт**: верификация merge kernel_cache_v2 сестрёнки от 2026-04-22
> **Финал**: v0.3.0 + v0.3.1 полностью закрывают KernelCache v2 scope

---

## 🟢 Финал: KernelCache v2 закрыт

| Репо | v0.3.0 | v0.3.1 | Tests |
|------|:------:|:------:|:-----:|
| core | ✅ (2ba4b37) | ✅ (ddc51be) | 113 PASS / 0 FAIL |
| spectrum | ✅ | — | green, HSACO cache |
| signal_generators | ✅ | ✅ (b7a7888) | 44 PASS / 0 FAIL |
| linalg | ✅ | — | 33 PASS / 0 FAIL |
| strategies | ✅ | — | 4 PASS / 0 FAIL |
| radar | ✅ | — | 6 PASS / 1 pre-existing¹ |

¹ `RangeAngle [T2]` — physical-math FAIL, не KC regression, отдельный таск.

---

## 📋 Этапы дня

### Фаза 1 — верификация merge (утро)

Собрал + прогнал тесты на всех 6 KC репо. Получил:
- **core** `[T_storage_services]` ошибку линковки — старый `TestKernelCacheService()` использовал name-only API, снятый в Phase A2.
  → Завернул в `#if 0` (коммит `2ba4b37`).
- **strategies/radar** через FetchContent ломались на `undefined rocblas_*` — Phase A2 добавил вызовы в `gpu_context.cpp`, но core линк не экспортировал `roc::rocblas`.
  → С OK Alex добавил `find_package(rocblas) + roc::rocblas PUBLIC` в [core/CMakeLists.txt](../../../core/CMakeLists.txt).

**Acceptance пройдено**: HSACO coexist по hash8, atomic write, corrupt detect, rocm-smi без утечек (2015MB → 1987MB после 5 прогонов).

### Фаза 2 — git tag v0.3.0 (6 репо)

После OK Alex — annotated tag `v0.3.0` на 6 KC-репо параллельно, все ушли в origin.

### Фаза 3 — «закрывай что не закрыто в Phase A-D» (полдень)

Прошёл 4 открытых пункта из TODO:

**#4 Физическое удаление устаревшего `TestKernelCacheService()`**
- [core/tests/test_storage_services.hpp](../../../core/tests/test_storage_services.hpp): 394 → 272 строк, `#if 0` блок вынесен, `#include <kernel_cache_service.hpp>` удалён.

**#5 Разведка spectrum disk-cache**
- Все 10 вызовов `CompileModule` в spectrum уже на Phase A2 path (KernelCacheService v2). Scope закрыт, ничего доделывать не нужно. HSACO пишутся с hash8 суффиксами.

**#2 SG Phase B2 — 6 генераторов на unified cache path**
- ДО: `ctx_(backend, "<Group>", "modules/signal_generators/kernels")` → legacy relative path.
- ПОСЛЕ: `ctx_(backend, "<Group>", drv_gpu_lib::ResolveCacheDir("signal_generators"))` → unified exe-relative path с chain fallback (`$DSP_CACHE_DIR → <exe>/kernels_cache → $HOME/.cache/dsp-gpu`).
- Файлы: `cw_generator_rocm.cpp`, `form_signal_generator_rocm.cpp`, `lfm_conjugate_generator_rocm.cpp`, `lfm_generator_analytical_delay_rocm.cpp`, `lfm_generator_rocm.cpp`, `noise_generator_rocm.cpp`.
- Коммит: `b7a7888`. Результат: HSACO в `signal_generators/build/tests/kernels_cache/signal_generators/<Group>/gfx1201/*.hsaco` — унифицировано с остальными репо.

**#3 Phase E1 CLI `dsp-cache-list`**
- [core/tools/dsp_cache_list.cpp](../../../core/tools/dsp_cache_list.cpp) + [README.md](../../../core/tools/README.md). Печатает таблицу кэшированных HSACO, группируя по module. Без CMake-правки (опция `DSP_BUILD_CLI_TOOLS` — под будущий OK Alex); manual hipcc build инструкции в README.
- **Bug fix** попутно: `KernelCacheService::ListEntries()` обходил только 2 уровня (`<module>/<arch>/*.hsaco`), но `GpuContext` пишет 3 (`<module>/<group>/<arch>/*.hsaco`). Переписал на `recursive_directory_iterator`.
- Коммит `ddc51be`. Smoke-test: CLI корректно показывает 5 SG kernels и 2 radar kernels.

### Фаза 4 — релиз v0.3.1

Два новых коммита → два tag'а `v0.3.1` на core и signal_generators (остальные 4 репо не менялись).

---

## ✅ Acceptance (по spec `KernelCache_v2_Proposal_2026-04-16.md`)

| # | Критерий | Статус |
|---|----------|--------|
| G1 | Key-based API, без name-only | ✅ (проверено test_kernel_cache_service 8/8) |
| G2 | HSACO coexist по hash8 в одном module/arch | ✅ (radar: 2 kernels, SG: 5 kernels) |
| G3 | FNV stability regression | ✅ (compile_key 5/5) |
| G4 | Atomic write + corrupted detection | ✅ (8 тестов) |
| G5 | Cache hit — без compile path | ✅ (лог `kernels loaded from cache (HSACO)`) |
| leak | rocm-smi stable | ✅ (2015MB → 1987MB после 5 запусков radar) |
| Phase B1 | ScriptGen disk cache | ✅ (merge 2e367bf) |
| Phase B2 | **Все 6 SG генераторов на unified disk cache** | ✅ **(b7a7888, 2026-04-23)** |
| Phase E1 | CLI `dsp-cache-list` | ✅ **(ddc51be, 2026-04-23)** |
| Phase E2 | Docs update `core/Doc/Services/Full.md` | ⬜ pending |

---

## 🛠️ Правки сегодня (суммарно)

| Репо | Commit | Файлы |
|------|--------|-------|
| core | `2ba4b37` | CMakeLists.txt (rocblas PUBLIC), test_storage_services.hpp (#if 0) |
| workspace | `39f261e` | MemoryBank/sessions, changelog, STATE |
| core | `ddc51be` | kernel_cache_service.cpp (ListEntries recursive), test_storage_services.hpp (cleanup), tools/dsp_cache_list.cpp + README.md |
| signal_generators | `b7a7888` | 6 генераторов → ResolveCacheDir |

Tags (origin):
- **v0.3.0** на 6 репо (core, spectrum, signal_generators, linalg, strategies, radar)
- **v0.3.1** на 2 репо (core, signal_generators) — polish patches

---

## ⏭️ Что осталось (не в scope KernelCache v2)

- **#1 radar RangeAngle [T2]** — physical-math regression-like; отдельный таск.
- **Phase E2** — обновить `core/Doc/Services/Full.md` (описать `CompileKey` API).
- **DSP_BUILD_CLI_TOOLS CMake option** — добавить при OK Alex.
- **stats / heterodyne v0.3.0** — не трогались KC merge, уедут в следующий релиз.
- **Profiler v2 Phase D** для 3 deferred репо (SG / heterodyne / strategies).

---

*Сдал: Кодо, день 2026-04-23. KernelCache v2 scope — **CLOSED**.*
