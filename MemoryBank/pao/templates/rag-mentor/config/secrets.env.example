# ============================================================================
#  rag-mentor secrets — COPY to config/secrets.env (НЕ КОММИТИТЬ!)
# ============================================================================

# Anthropic API (опционально — если без Claude Code wrapper)
ANTHROPIC_API_KEY=sk-ant-XXXXXXXXXXXXXXXXXXX

# PostgreSQL
POSTGRES_PASSWORD=<set-locally>

# Qdrant (если включён auth)
QDRANT_API_KEY=

# Sync — bare remote SSH password / key
RAG_PAO_BARE_REMOTE_SSH_KEY=~/.ssh/id_ed25519
