# TASK — синк глобального конфига Windows ↔ Debian (не передаётся через github)

> **Создан:** 2026-06-06 · Кодо → Alex
> **Зачем:** `~/.claude/CLAUDE.md` и прочий global-конфиг живут ВНЕ git-репо → не едут через
> github. После правок на одной машине надо вручную переносить на другую.

---

## A. Применить на Debian сегодняшние правки global CLAUDE.md  ⏳

На Windows (`C:\Users\user\.claude\CLAUDE.md`) добавлены 2 урока (2026-06-06):
1. **§8 «Сначала — нужна ли задача вообще»** — новый инструмент/модель + рабочий стек →
   один вопрос «менять только если лучше?»; не существует/не влезает → сказать сразу, не копать.
2. **Новая секция «📦 Скачивание / установка»** — полный список файлов+зависимостей+ГБ в один
   заход (Alex возит SSD дом↔работа).

**Действие на Debian:**
```bash
# вариант 1 — скопировать целиком (если Debian-версия не расходится):
#   перенести C:\Users\user\.claude\CLAUDE.md → ~/.claude/CLAUDE.md
# вариант 2 — вписать те же 2 блока руками (если есть Debian-специфика)
```
- [ ] Перенесено/вписано на Debian `~/.claude/CLAUDE.md`
- [ ] Сверить `*Last updated*` = 2026-06-06 на обеих машинах

---

## B. Общий реестр конфига, который НЕ едет через github  📋

Инвентаризовать всё, что надо синкать руками Windows↔Debian (живёт вне git-репо):

| Что | Windows | Debian | Как синкать |
|-----|---------|--------|-------------|
| Global CLAUDE.md | `C:\Users\user\.claude\CLAUDE.md` | `~/.claude/CLAUDE.md` | вручную (см. A) |
| Claude memory | `C:\Users\user\.claude\projects\E--DSP-GPU\memory\` | `~/.claude/projects/.../memory/` | ? проверить путь |
| `~/.claude/settings*.json` | `.claude/settings.local.json` | `~/.claude/...` | разнятся (платформа) — сверять выборочно |
| Глобальные skills/agents (если вне репо) | `~/.claude/` | `~/.claude/` | проверить что в репо, что нет |

- [ ] Заполнить таблицу реальными путями (Read/ls на обеих машинах)
- [ ] Решить по каждому: автосинк / вручную / не нужно
- [ ] Зафиксировать рецепт в `MemoryBank/specs/` или правило

> Project-rules (`.claude/rules/`) и MemoryBank — **едут через github**, в этот реестр НЕ входят.

---
*2026-06-06 · Кодо*
</content>
